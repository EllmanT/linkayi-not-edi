import { GetTableDataPipe } from './get-table-data.pipe';

describe('GetTableDataPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTableDataPipe();
    expect(pipe).toBeTruthy();
  });
});
